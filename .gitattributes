# Normalize line endings to LF in the repo so Windows (CRLF) and the Linux CI
# runner agree. Without this, autocrlf churns diffs and can break format checks.
* text=auto eol=lf

# Binary assets — never touch.
*.ico binary
*.png binary
*.jpg binary
*.webp binary
